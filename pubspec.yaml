name: bigboss_eren
description: "BigBoss Eren Kuyumculuk POS Sistemi"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # İhtiyacımız olan paketler
  cupertino_icons: ^1.0.2
  http: ^1.2.0
  webview_flutter: ^4.7.0
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  firebase_auth: ^6.1.3
  intl: ^0.18.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # HTML dosyamızı buraya tanıtıyoruz
  assets:
    - assets/pos.html